{
  "name": "energy-flow-card",
  "version": "0.9.0",
  "description": "Custom Home Assistant Lovelace card that visualizes live energy flows and device-level power.",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=esm --target=es2021 --outfile=dist/energy-flow-card.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "esbuild": "*",
    "tsx": "*",
    "typescript": "*"
  }
}
